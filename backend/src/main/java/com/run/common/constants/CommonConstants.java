package com.run.common.constants;

import java.util.UUID;

public class CommonConstants {
    public static final UUID ROOT_FOLDER_ID = new UUID(0, 0);
    public static final String ROOT_FOLDER_ID_STR = ROOT_FOLDER_ID.toString();
}
